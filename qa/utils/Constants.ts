// utils/Constants.ts
import 'dotenv/config';

export const USER = {
};

export const URL = {
  BASE_URL: process.env.BASE_URL || '',
};

export const AlertMessage = {
  LOGIN_ERROR_NoInput: '아이디와 비밀번호를 입력해주세요.',
};
